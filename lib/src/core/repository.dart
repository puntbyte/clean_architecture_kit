/// An abstract interface class representing a generic Repository.
///
/// The Repository pattern is used to decouple business logic from data access
/// layers. This interface serves as a contract for all concrete repositories in
/// an application.
abstract interface class Repository {
  const Repository();
}
